// next.config.ts
//
// Next.js configuration:
//   - No special config needed for MVP
//   - When image uploads are added, configure `images.domains` to allow
//     whichever storage bucket URLs are used (e.g. Firebase Storage or S3)
//   - Set NEXT_PUBLIC_API_URL env var to point to the FastAPI backend
//     (defaults to http://localhost:8000 in development)
