# BuffBites — API Documentation

**Authors:** Ishita Pawar, Rajvardhan Patil, Sejal Hukare
**Base URL:** `http://localhost:8000` (local dev) — `https://buffbites-backend.fly.dev` (production, Fly.io)
**Interactive Docs:** `http://localhost:8000/docs` (Swagger UI, auto-generated by FastAPI)

---

## Overview

BuffBites has five groups of endpoints:

1. **Combos / Menu** (`/api/combos/`, `/api/menu`) — AI-generated meal combos and raw menu data. Loads pre-scraped menu JSON, classifies stations, hard-filters by dietary preference, calls `claude-haiku-4-5` with structured output, validates with Pydantic v2, cross-checks dishes against the scraped menu, and returns 9 combos (3 per meal period). Combos are personalizable (protein goal, dietary focus, priority nutrients, dietary restrictions).
2. **Users** (`/api/users/`) — User profiles stored in MongoDB. Includes dietary preferences, calorie goal per meal, a running meal log, and saved favorites.
3. **Community** (`/api/community/`) — User-submitted combos with 24-hour expiry, upvote/downvote voting, and daily + weekly trends leaderboards. Firebase auth required to publish, edit, delete, or vote.
4. **Comments** (`/api/community/combos/{id}/comments`) — Threaded comments on community combos. Auth required to post/delete.
5. **Drafts** (`/api/drafts/`) — Private drafts that live permanently under a user's profile until deleted or published to the community feed.

**"Today" is resolved in Mountain Time** (the dining halls are in Boulder, CO). Combo generation is **rate-limited per IP** (20 requests / 60s → `429`) and **cached** in MongoDB for 24h, keyed by `(dining, date, goals, dietary prefs)` — a cache hit skips the Claude call entirely.

---

## Testing the API

### 1. Unit tests (no server needed)

```bash
cd backend
PYTHONPATH=. venv/bin/pytest tests/ -v
```

Covers combo model validation, dish verification, station classification, and item filtering. No API keys or running server required.

### 2. Start the server

```bash
cd backend
venv/bin/uvicorn main:app --reload
```

### 3. Swagger UI (easiest)

Open `http://localhost:8000/docs` — fill in `dining` and `date` and click **Execute**. No curl needed.

### 4. curl examples

```bash
# Health check
curl http://localhost:8000/

# Raw menu (no Claude call — fast)
curl "http://localhost:8000/api/menu?dining=c4c&date=2026-04-28"

# Generate combos (calls Claude — needs ANTHROPIC_API_KEY in .env)
curl "http://localhost:8000/api/combos/generate?dining=c4c&date=2026-04-28"
```

### 5. Structured request logs

Every request prints a JSON log line to stdout — useful for checking latency and status codes without a dedicated monitoring tool:

```json
{"method": "GET", "path": "/api/combos/generate", "status": 200, "duration_ms": 3241.5, "event": "request", "level": "info", "timestamp": "2026-04-28T19:57:57Z"}
```

Hallucinated dishes (names not on the scraped menu) are **dropped** before the response is built; any combo left with fewer than 2 real dishes is skipped. Drops and single-station combos are logged to **stderr** and do not cause a `500`.

---

## Endpoints

---

### `GET /`

Root ping. Returns a status message confirming the API process is running.

**Request**
```
GET http://localhost:8000/
```

**Response `200`**
```json
{
  "message": "BuffBites API is running"
}
```

---

### `GET /health`

Deep health check. Pings MongoDB and returns database connectivity status.

**Request**
```
GET http://localhost:8000/health
```

**Response `200` — healthy**
```json
{
  "status": "ok",
  "db": "ok"
}
```

**Response `503` — database unreachable**
```json
{
  "status": "degraded",
  "db": "unreachable"
}
```

---

### `GET /api/menu`

Returns raw menu items grouped by station for a given dining hall and date. Applies the same station classifier and item filter as combo generation — excluded stations (condiments, sides, bread, beverages, etc.) and component items (sauces, diced/sliced toppings) are stripped out. No Claude call is made.

#### Query Parameters

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `dining` | Yes | string | Dining hall key. One of: `alley`, `c4c`, `libby`, `seec`, `sewall`, `village_center` |
| `date` | No | string | Date in `YYYY-MM-DD` format. Defaults to today. |

#### Response `200`

```json
{
  "dining_location": "Center for Community (C4C)",
  "date": "2026-04-28",
  "day_of_week": "Tuesday",
  "categories": {
    "Smoke n' Grill": [
      {
        "name": "Grilled Chicken Breast",
        "description": "",
        "serving_size": "4 oz",
        "calories": 180,
        "allergens": [],
        "dietary_labels": ["Halal"],
        "is_vegan": false,
        "is_vegetarian": false,
        "nutrition": { "protein_g": 34.0, "fat_g": 4.0, "carbohydrates_g": 0.0 }
      }
    ]
  }
}
```

Used by the Create page and the Full Menu view to populate station and dish pickers.

---

### `GET /api/menu/nutrition`

Returns per-dish nutrition for a specific list of dish names — used to enrich combo cards with calories and macros without re-sending the whole menu. No Claude call.

#### Query Parameters

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `dining` | Yes | string | Dining hall key |
| `date` | No | string | `YYYY-MM-DD`, defaults to today (Mountain Time) |
| `dishes` | Yes | string | Comma-separated dish names to look up |

#### Response `200`

A map of requested dish name → nutrition (missing dishes map to `{}`):

```json
{
  "Grilled Chicken Breast": {
    "calories": 180,
    "protein_g": 34.0,
    "fat_g": 4.0,
    "carbs_g": 0.0,
    "serving_size": "4 oz"
  },
  "Made-Up Dish": {}
}
```

---

### `GET /api/combos/generate`

Generates 9 AI-powered meal combos for a given CU Boulder dining hall and date.
Returns exactly **3 combos per meal period**: Breakfast, Lunch, and Dinner.

Each combo contains 2–6 dishes drawn from the day's scraped menu, with cross-station mixing encouraged. Every dish includes the station it comes from. The 3rd Dinner combo is always a dessert specialty.

---

#### Query Parameters

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `dining` | Yes | string | Dining hall key. One of: `alley`, `c4c`, `libby`, `seec`, `sewall`, `village_center` |
| `date` | No | string | Date in `YYYY-MM-DD` format. Defaults to today (Mountain Time) if omitted. |
| `protein_goal` | No | integer | Target protein per meal in grams — combos aim to meet it. |
| `dietary_focus` | No | string | One of: `balanced`, `high-protein`, `low-carb`, `weight-loss`, `muscle-gain`, `endurance`. |
| `priority_nutrients` | No | string | Comma-separated, any of: `iron`, `calcium`, `vitamin-d`, `fiber`, `omega-3`, `b12`, `zinc`. |
| `dietary_preferences` | No | string | Comma-separated, any of: `vegan`, `vegetarian`, `gluten-free`, `halal`. Items violating these are hard-filtered out of the pool before Claude sees them. |

> Each unique combination of parameters has its own 24h cache entry.

---

#### Request Examples

```bash
# C4C — specific date
curl "http://localhost:8000/api/combos/generate?dining=c4c&date=2026-03-17"

# Personalized — high-protein, vegetarian, 40g protein target, prioritize iron
curl "http://localhost:8000/api/combos/generate?dining=c4c&date=2026-03-17&dietary_focus=high-protein&dietary_preferences=vegetarian&protein_goal=40&priority_nutrients=iron"

# Village Center — defaults to today
curl "http://localhost:8000/api/combos/generate?dining=village_center&date=2026-03-08"

# The Alley
curl "http://localhost:8000/api/combos/generate?dining=alley&date=2026-03-17"

# Sewall
curl "http://localhost:8000/api/combos/generate?dining=sewall&date=2026-03-17"

# Libby
curl "http://localhost:8000/api/combos/generate?dining=libby&date=2026-03-17"
```

---

#### Success Response `200`

```json
{
  "dining_location": "Center for Community (C4C)",
  "date": "2026-03-17",
  "day_of_week": "Tuesday",
  "combos": {
    "Breakfast": [
      {
        "title": "The Morning Buff",
        "dishes": [
          { "name": "Scrambled Eggs", "station": "Hot Bar" },
          { "name": "Seasonal Fruit Cup", "station": "Wholesome Field Fruit" },
          { "name": "Whole Grain Toast", "station": "Bakery" }
        ],
        "description": "A light, balanced way to start the day — protein from eggs, natural sugars from fruit, and slow-burning carbs from whole grain toast.",
        "approximate_calories": 430,
        "tags": ["vegetarian", "high-protein", "balanced"]
      },
      {
        "title": "Buff Power Start",
        "dishes": [
          { "name": "Oatmeal", "station": "Hot Bar" },
          { "name": "Hard Boiled Egg", "station": "Salad Bar" }
        ],
        "description": "High-fiber oatmeal paired with a protein-rich egg for sustained morning energy.",
        "approximate_calories": 310,
        "tags": ["vegetarian", "high-fiber", "low-calorie"]
      },
      {
        "title": "Golden Morning",
        "dishes": [
          { "name": "Pancakes", "station": "Grill" },
          { "name": "Turkey Sausage", "station": "Hot Bar" },
          { "name": "Orange Juice", "station": "Beverage" }
        ],
        "description": "Classic diner-style breakfast with a leaner protein swap.",
        "approximate_calories": 620,
        "tags": ["high-protein", "comfort-food"]
      }
    ],
    "Lunch": [
      {
        "title": "The Flatiron",
        "dishes": [
          { "name": "Grilled Chicken Sandwich", "station": "Smoke n' Grill" },
          { "name": "Caesar Salad", "station": "Salad Bar" }
        ],
        "description": "A filling, high-protein midday combo with fresh greens to balance the grilled sandwich.",
        "approximate_calories": 680,
        "tags": ["high-protein", "balanced"]
      },
      {
        "title": "Pasta Buff",
        "dishes": [
          { "name": "Penne Marinara", "station": "Pasta Bar" },
          { "name": "Garlic Bread", "station": "Bakery" },
          { "name": "Minestrone Soup", "station": "Soup" }
        ],
        "description": "Carb-forward Italian combo — great before an afternoon workout or long study session.",
        "approximate_calories": 750,
        "tags": ["vegan", "high-carb", "comfort-food"]
      },
      {
        "title": "Build-Your-Buff Bowl",
        "dishes": [
          { "name": "Brown Rice", "station": "Hot Bar" },
          { "name": "Smokey Tofu", "station": "Smoke n' Grill" },
          { "name": "Roasted Vegetables", "station": "Hot Bar" }
        ],
        "description": "Plant-based power bowl mixing smoky tofu with roasted veg over wholesome brown rice.",
        "approximate_calories": 590,
        "tags": ["vegan", "high-protein", "low-carb"]
      }
    ],
    "Dinner": [
      {
        "title": "The Summit Plate",
        "dishes": [
          { "name": "Herb Roasted Chicken", "station": "Featured Entree" },
          { "name": "Mashed Potatoes", "station": "Hot Bar" },
          { "name": "Steamed Broccoli", "station": "Hot Bar" }
        ],
        "description": "A classic, well-rounded dinner plate with lean protein, complex carbs, and greens.",
        "approximate_calories": 780,
        "tags": ["high-protein", "halal", "balanced"]
      },
      {
        "title": "Vegan Nightcap",
        "dishes": [
          { "name": "Lentil Soup", "station": "Soup" },
          { "name": "Quinoa Salad", "station": "Salad Bar" },
          { "name": "Whole Wheat Roll", "station": "Bakery" }
        ],
        "description": "Light but protein-rich vegan dinner packed with fiber and plant-based nutrients.",
        "approximate_calories": 510,
        "tags": ["vegan", "high-fiber", "low-calorie"]
      },
      {
        "title": "East Boulder Fusion",
        "dishes": [
          { "name": "Teriyaki Salmon", "station": "Featured Entree" },
          { "name": "Fried Rice", "station": "Asian Station" },
          { "name": "Edamame", "station": "Salad Bar" }
        ],
        "description": "Asian-inspired dinner with heart-healthy salmon, savory fried rice, and protein-packed edamame.",
        "approximate_calories": 860,
        "tags": ["high-protein", "omega-3", "gluten-free"]
      }
    ]
  }
}
```

---

#### Response Fields

**Top-level:**

| Field | Type | Description |
|-------|------|-------------|
| `dining_location` | string | Full name of the dining hall |
| `date` | string | Date in `YYYY-MM-DD` format |
| `day_of_week` | string | e.g. `"Tuesday"` |
| `combos` | object | Map of meal period → array of 3 combos |

**`combos` object keys:** `Breakfast`, `Lunch`, `Dinner` — each is an array of exactly 3 `Combo` objects.

**`Combo` object:**

| Field | Type | Constraints | Description |
|-------|------|-------------|-------------|
| `title` | string | — | Fun, catchy combo name |
| `dishes` | array of `Dish` | 2–6 items | Menu items included in this combo |
| `description` | string | — | Why this combo works nutritionally or flavor-wise |
| `approximate_calories` | integer | — | Estimated total calorie count |
| `tags` | array of string | — | e.g. `"high-protein"`, `"vegan"`, `"low-carb"`, `"halal"`, `"gluten-free"` |

**`Dish` object:**

| Field | Type | Description |
|-------|------|-------------|
| `name` | string | Exact dish name as it appears on the menu |
| `station` | string | Dining station where this item is served (e.g. `"Smoke n' Grill"`, `"Salad Bar"`) |

---

### Users

| Method | Endpoint | Auth | Description |
|--------|----------|------|-------------|
| POST | `/api/users/` | No | Create a new user profile after Firebase sign-in |
| GET | `/api/users/check-username/{username}` | No | Check if a username is available |
| GET | `/api/users/{firebase_uid}` | No | Get a user's full profile including meal log + favorites |
| PUT | `/api/users/{firebase_uid}` | No | Update any profile fields (partial update via `$set`) |
| POST | `/api/users/{firebase_uid}/meal-log` | No | Append an eaten meal entry to the user's log |
| DELETE | `/api/users/{firebase_uid}/meal-log` | No | Remove a logged meal by its `logged_at` timestamp (query param) |
| POST | `/api/users/{firebase_uid}/favorites` | No | Save a combo to the user's favorites |
| DELETE | `/api/users/{firebase_uid}/favorites` | No | Remove a favorite by `title`, `dining_hall`, `date` (query params) |

**`POST /api/users/` request body:**
```json
{
  "firebase_uid": "abc123",
  "email": "raj@colorado.edu",
  "username": "buffraj",
  "dietary_preferences": ["vegan", "halal"],
  "restrictions": ["gluten-free"],
  "avatar": "avatar_3",
  "preferred_calories_per_meal": 700,
  "default_dining_hall": "c4c",
  "nutrition_goals": {
    "protein_g_per_meal": 40,
    "dietary_focus": "high-protein",
    "priority_nutrients": ["iron", "fiber"]
  }
}
```
All fields except `firebase_uid`, `email`, and `username` are optional and default to empty/`null`.

**`GET /api/users/check-username/{username}` response:**
```json
{ "available": true }
```

**`GET /api/users/{firebase_uid}` response:**
```json
{
  "firebase_uid": "abc123",
  "email": "raj@colorado.edu",
  "username": "buffraj",
  "dietary_preferences": ["vegan"],
  "restrictions": [],
  "avatar": "avatar_3",
  "karma": 42,
  "created_at": "2026-01-15T10:00:00Z",
  "preferred_calories_per_meal": 700,
  "default_dining_hall": "c4c",
  "nutrition_goals": {
    "protein_g_per_meal": 40,
    "dietary_focus": "high-protein",
    "priority_nutrients": ["iron", "fiber"]
  },
  "meal_log": [
    {
      "title": "The Flatiron",
      "calories": 680,
      "protein_g": 35,
      "date": "2026-06-03",
      "dining_hall": "c4c",
      "meal_period": "Lunch",
      "logged_at": "2026-06-03T13:22:00Z"
    }
  ],
  "favorites": [
    {
      "title": "The Flatiron",
      "dining_hall": "c4c",
      "date": "2026-06-03",
      "description": "A hearty high-protein lunch.",
      "approximate_calories": 680,
      "tags": ["high-protein"],
      "dishes": [{ "name": "Grilled Chicken", "station": "Smoke n' Grill" }],
      "saved_at": "2026-06-03T13:25:00Z"
    }
  ]
}
```

**`POST /api/users/{firebase_uid}/meal-log` request body:**
```json
{
  "title": "The Flatiron",
  "calories": 680,
  "date": "2026-06-03",
  "dining_hall": "c4c",
  "meal_period": "Lunch"
}
```

**Notes:**
- `preferred_calories_per_meal` is optional; used on the profile page to track daily calorie intake against a per-meal goal
- `meal_log` grows unbounded — entries are appended via MongoDB `$push`
- Username must be 3–20 characters; checked for uniqueness on create and update

---

### Community

| Method | Endpoint | Auth | Description |
|--------|----------|------|-------------|
| POST | `/api/community/combos` | Yes | Publish a combo to the community feed |
| GET | `/api/community/combos` | No | Get all active combos sorted by upvotes |
| GET | `/api/community/combos/user/{firebase_uid}` | No | Get a specific user's active combos |
| GET | `/api/community/combos/{combo_id}` | No | Get a single combo by ID |
| PUT | `/api/community/combos/{combo_id}` | Yes (owner) | Edit title, description, tags, dishes, or notes |
| DELETE | `/api/community/combos/{combo_id}` | Yes (owner) | Delete a combo |
| POST | `/api/community/combos/{combo_id}/vote` | Yes | Upvote or downvote — one vote per user, no undo |
| GET | `/api/community/trends` | No | Top 20 combos by upvotes for the current day |
| GET | `/api/community/trends/weekly` | No | Top combos by upvotes for the current week |
| POST | `/api/community/combos/{combo_id}/comments` | Yes | Add a comment (`{ "text": "..." }`) to a combo |
| GET | `/api/community/combos/{combo_id}/comments` | No | List comments on a combo |
| DELETE | `/api/community/comments/{comment_id}` | Yes (author) | Delete a comment |

**`POST /api/community/combos` request body** (query param: `?username=buffraj`):
```json
{
  "title": "The Midnight Special",
  "dining_hall": "alley",
  "date": "2026-03-25",
  "dishes": [
    { "name": "Mac & Cheese", "station": "Hot Bar", "servings": 1 },
    { "name": "Garlic Bread", "station": "Bakery", "servings": 2 }
  ],
  "tags": ["vegetarian", "comfort-food"],
  "description": "Late night comfort food done right",
  "images": [],
  "notes": "Add hot sauce for extra kick"
}
```

**`GET /api/community/combos` query params:**

| Parameter | Required | Description |
|-----------|----------|-------------|
| `dining_hall` | No | Filter by dining hall key |
| `firebase_uid` | No | If provided, each combo includes `has_voted: bool` for that user |

**`PUT /api/community/combos/{combo_id}` request body** (all fields optional):
```json
{
  "title": "Updated Title",
  "description": "New description",
  "tags": ["vegan"],
  "dishes": [{ "name": "Salad", "station": "Salad Bar", "servings": 1 }],
  "notes": "Updated notes"
}
```

**`POST /api/community/combos/{combo_id}/vote` query param:** `?vote_type=upvote` or `?vote_type=downvote`

**Notes:**
- Published combos expire after 24 hours — they appear in community and trends until then
- One vote per user per combo; re-voting returns `409 Already voted`
- Only the author can edit or delete their combo (`403` otherwise)
- `username` query param on publish falls back to the Firebase display name if omitted

---

---

### Drafts

| Method | Endpoint | Auth | Description |
|--------|----------|------|-------------|
| POST | `/api/drafts/` | Yes | Save a new draft |
| GET | `/api/drafts/{firebase_uid}` | Yes (owner) | Get all drafts for a user |
| PUT | `/api/drafts/{draft_id}` | Yes (owner) | Update an existing draft |
| DELETE | `/api/drafts/{draft_id}` | Yes (owner) | Delete a draft |
| POST | `/api/drafts/{draft_id}/publish` | Yes (owner) | Publish draft → creates community combo, deletes draft |

**Notes:**
- Drafts never expire — they live under the user's profile permanently until deleted or published
- Publishing a draft creates a community combo and deletes the draft
- Published combos expire after 24 hours and cannot be republished
- Only the draft owner can view, edit, delete or publish their drafts
- All draft endpoints require Firebase auth

---

## Error Responses

| Status | When | Response body |
|--------|------|---------------|
| `400` | `dining` param is missing or not a valid key | `{ "detail": "Invalid dining location. Must be one of: ..." }` |
| `404` | No menu data exists for that dining hall on that date (data refreshes daily — try again after the 8 AM UTC cron) | `{ "detail": "No menu found for ... on ..." }` |
| `404` | Not enough menu items to build combos after filtering | `{ "detail": "Not enough menu items to build combos" }` |
| `429` | More than 20 combo requests from one IP in 60s | `{ "detail": "Too many combo requests. Please wait a moment and try again." }` |
| `500` | Scraped JSON file could not be read | `{ "detail": "Failed to load menu data" }` |
| `500` | Claude call / parsing / validation failed | `{ "detail": "Combo generation failed: ..." }` |

> **Note on dish verification:** Hallucinated dishes (not found in today's menu) are dropped from combos before the response is built; a combo left with fewer than 2 real dishes is skipped entirely. These drops are logged to the server console (`stderr`) but never cause a `500`. Check server logs if a combo looks shorter than expected.

---

## Dining Hall Keys

| Key | Full Name | Multi-Station |
|-----|-----------|---------------|
| `alley` | The Alley at Farrand | No |
| `c4c` | Center for Community (C4C) | Yes |
| `libby` | Libby Dining | No |
| `seec` | SEEC Cafe | No |
| `sewall` | Sewall Dining | No |
| `village_center` | Village Center Dining | Yes |

Multi-station dining halls (C4C, Village Center) have items from multiple named stations (e.g. `"Smoke n' Grill"`, `"Pasta Bar"`, `"Salad Bar"`). Combos are encouraged to mix items across stations.

---

## Pydantic Models (`backend/pydantic_models/combo_models.py`)

All validation runs through these models before any response leaves the server.

| Model | Purpose |
|-------|---------|
| `Dish` | Single menu item — `name` + `station` |
| `Combo` | One combo — validates 2–6 dishes |
| `CombosMap` | Holds `Breakfast`, `Lunch`, `Dinner` — validates exactly 3 combos each |
| `ComboResponse` | Top-level response — `dining_location`, `date`, `day_of_week`, `combos` |
| `DishVerificationError` | Describes a failed dish check (hallucination or wrong station) |

**`verify_combos(response, day_menu)`** — iterates every dish in the response, looks up its name (case-insensitive) in the scraped menu index, and returns a list of `DishVerificationError`. Empty list = all clear.

---

## Menu Data Format

Scraped JSON files live in `scraping_scripts/data/` and are refreshed daily by GitHub Actions. Each file covers a **6-week rolling window** starting from Monday two weeks before the scrape date, so today's menu is always present. Each file has this top-level shape:

```json
{
  "dining_location": "Center for Community (C4C)",
  "url": "https://...",
  "scrape_start_date": "2026-04-14",
  "menus": [
    {
      "date": "2026-03-17",
      "day_of_week": "Tuesday",
      "categories": {
        "Smoke n' Grill": [ ... ],
        "Pasta Bar": [ ... ],
        "Salad Bar": [ ... ]
      }
    }
  ]
}
```

Each item within a category:

```json
{
  "name": "Smokey Tofu",
  "description": "",
  "serving_size": "4 oz",
  "calories": 181.0,
  "allergens": ["Soy"],
  "dietary_labels": ["Vegan"],
  "is_vegan": true,
  "is_vegetarian": true,
  "nutrition": {
    "calories": 181.0,
    "fat_g": 7.0,
    "protein_g": 16.0,
    "carbohydrates_g": 15.0,
    "sodium_mg": 1198.0
  }
}
```
